# Place all the behaviors and hooks related to the matching controller here.
# All this logic will automatically be available in application.js.
# You can use CoffeeScript in this file: http://coffeescript.org/

#
# onReady = (callback) ->
#   theinterval = window.setInterval((->
#     if document.getElementsByClassName('threedee')[0] != undefined
#       window.clearInterval theinterval
#       callback.call this
#     return
#   ), 1000)
#   return
#
# setVisible = (selector, visible) ->
#   document.querySelector(selector).style.display = if visible then 'block' else 'none'
#   return
#
# onReady ->
#   setVisible 'canvas', true
#   setVisible '#loading', false
#   return
